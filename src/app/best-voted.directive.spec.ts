import { BestVotedDirective } from './best-voted.directive';

describe('BestVotedDirective', () => {
  it('should create an instance', () => {
    const directive = new BestVotedDirective();
    expect(directive).toBeTruthy();
  });
});